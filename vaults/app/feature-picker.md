# Feature Picker

A sub-section of [[full-create-classes-and-features-section]] headed "Features" with a counter "0 / 4 (+1 Training)".

It contains a text input ("Enter feature name..." placeholder) with a disabled "Add" button, plus a separate "Training Feature" line described as "free, no prerequisites" with its own text input ("Enter training feature name..." placeholder).

A validation message reads: "Level 1 trainers should have 5 features (4 + 1 Training) (have 0)".
