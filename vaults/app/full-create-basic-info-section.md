# Full Create Basic Info Section

The first section of [[full-create-mode]]. Contains the same fields as [[quick-create-mode]]'s basic info: Name (required text field), Character Type (dropdown: Player Character or NPC, defaults to NPC), Level (number spinner, defaults to 1), Location (text field with "e.g., Mesagoza" placeholder), and a [[trainer-sprite-chooser]].
