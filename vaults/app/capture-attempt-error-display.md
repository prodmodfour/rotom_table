When the capture attempt API call fails, an error message appears below the [[capture-attempt-accuracy-roll]] result in the [[capture-section-on-wild-pokemon-cards]]. The error text shows the HTTP method, endpoint, and status code — for example: `[POST] "/api/capture/attempt": 404 Server Error`. The error message persists until the GM clicks the [[capture-attempt-new-attempt-button]].
