# Battle Grid Dimensions Display

A label in the [[battle-grid]] header showing the grid size as "width×height" (e.g. "20×15"). The dimensions update when the grid is resized through the [[battle-grid-settings-panel]].

## See also

- [[grid-config-type]] — the width and height values come from this config
