New Day is a pure counter reset with no implicit extended rest. It resets daily counters (restMinutesToday, injuriesHealedToday) but does NOT heal HP or clear persistent conditions.

The GM must trigger extended rest separately. This preserves scenarios where a new day occurs without rest (forced march, captured) and keeps [[separate-mechanics-stay-separate]].
