Ghost-type Pokemon cannot be Stuck or Trapped. This is a type-based immunity that the app must enforce automatically — when a move or effect would apply Stuck or Trapped to a Ghost type, it simply fails.

This interacts with [[stuck-blocks-movement-not-actions]] and is enforced through the same pattern as other type immunities: [[server-enforcement-with-gm-override]].
