The Damage Base (DB) chart maps DB values 1 through 28 to damage ranges. Each entry provides `min`, `avg`, and `max` values.

The [[nine-step-damage-formula]] uses the DB value from a move's data to look up the base damage at step 1.

## See also

- [[nine-step-damage-formula]] — consumes this chart as the first calculation step
