# Inline Method

A [[composing-methods-techniques|composing methods]] [[refactoring-techniques|technique]]. Replace calls to a method with the method's body, then delete the method.

Use when the method's implementation is already obvious and the method name adds no clarity — the abstraction obscures rather than helps.
