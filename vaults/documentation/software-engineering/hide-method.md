# Hide Method

A [[simplifying-method-calls-techniques|simplifying method calls]] [[refactoring-techniques|technique]]. When a method isn't used by other classes or is used only within its own class hierarchy, reduce its visibility to private or protected.

Smaller public APIs are easier to understand and maintain.
