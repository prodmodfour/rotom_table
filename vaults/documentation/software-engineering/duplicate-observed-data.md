# Duplicate Observed Data

An [[organizing-data-techniques|organizing data]] [[refactoring-techniques|technique]]. When domain data is stored in GUI-responsible classes, separate it into dedicated domain classes and keep the two synchronized.

Decouples business logic from presentation so each can change independently.
