# Replace Array with Object

An [[organizing-data-techniques|organizing data]] [[refactoring-techniques|technique]]. When an array contains elements of different types where each position has a specific meaning, replace it with an object that has a named field for each element.

Named fields are self-documenting and type-safe, unlike positional array slots.
