// Player view types

/**
 * Available tabs in the player bottom navigation.
 * Used by PlayerNavBar and the player page for tab state management.
 */
export type PlayerTab = 'character' | 'team' | 'encounter'
