---
id: bug-058
title: "No separate HP loss pathway; Belly Drum/Life Orb triggers massive damage injuries"
priority: P2
severity: MEDIUM
status: in-progress
domain: combat
source: combat-audit.md (session 121, R031)
created_by: slave-collector (plan-matrix-1772722531)
created_at: 2026-03-05
affected_files:
  - app/server/services/damage.service.ts
  - app/server/services/combat.service.ts
---

## Summary

PTU distinguishes "HP loss" (e.g., Belly Drum, Life Orb recoil) from "damage" (attack-based). HP loss should bypass defense calculations and injury checks. Currently all HP reduction goes through the damage pathway, which incorrectly triggers massive damage injury checks for effects like Belly Drum (50% HP loss).

## Suggested Fix

Add a `hpLoss` flag or separate endpoint that reduces HP without triggering injury/defense calculations.

## Impact

Belly Drum and Life Orb users receive incorrect injury penalties. Cross-domain overlap with healing audit finding R012/R035 (Pain Split/Endeavor).

## Resolution Log

### Implementation (2026-03-06)

Added `HpReductionType` enum (`'damage' | 'hpLoss' | 'setHp'`) to the `calculateDamage` function. The `lossType` parameter controls whether massive damage injuries and heavily injured penalty are applied.

**Commits:**
- `c22c22db` — feat: add HpReductionType to calculateDamage
- `a6eb465d` — feat: damage endpoint accepts lossType parameter
- `baa74b59` — feat: frontend composable accepts lossType
- `9a34fcd2` — fix: tick damage and weather damage use hpLoss type
- `f13aa937` — fix: weather ability damage uses hpLoss type
- `178a4411` — feat: propagate lossType through store and handlers
- `49b8182f` — feat: add HP reduction type selector to GM damage controls

**Files changed:**
- `app/server/services/combatant.service.ts` — HpReductionType, calculateDamage lossType param
- `app/server/api/encounters/[id]/damage.post.ts` — accept lossType, skip heavily injured for non-damage
- `app/server/api/encounters/[id]/next-turn.post.ts` — tick/weather damage uses hpLoss
- `app/server/utils/turn-helpers.ts` — weather ability damage uses hpLoss
- `app/composables/useEncounterCombatActions.ts` — applyDamage accepts lossType
- `app/composables/useEncounterActions.ts` — handleDamage accepts lossType
- `app/stores/encounter.ts` — applyDamage forwards lossType
- `app/components/encounter/CombatantGmActions.vue` — loss type dropdown selector
- `app/components/encounter/CombatantCard.vue` — forward lossType in damage emit
- `app/components/gm/CombatantSides.vue` — forward lossType in damage emit

### D2 Fix Cycle (2026-03-06) — code-review-352

Addressed all issues from code-review-352:

- **HIGH-002** (f3d24b91): Reset `lossTypeInput` to `'damage'` after emitting damage event in CombatantGmActions.vue. Sticky selector caused invisible gameplay errors.
- **HIGH-001** (3fd51167): Moved `HpReductionType` from combatant.service.ts to `~/types/combat.ts` as canonical location. Replaced 6 inline union definitions with imported type. Re-export from combatant.service.ts for backward compat. Updated imports in damage.post.ts and useEncounterCombatActions.ts to use canonical path.
- **MED-001**: Verified decree-need-053 already existed and was addressed by decree-054 (split hpLoss into recoil vs self-cost). No new decree-need required.
- **MED-002** (c5f8b4f6): Added 21 unit tests for `calculateDamage` lossType parameter covering all three branches (damage/hpLoss/setHp), default backward compat, massive damage skipping, temp HP preservation, marker injury application, and injury accumulation.

**D2 Commits:**
- `f3d24b91` — fix: reset lossType selector to 'damage' after applying HP reduction
- `3fd51167` — refactor: move HpReductionType to ~/types/combat.ts as single source of truth
- `c5f8b4f6` — test: add calculateDamage lossType unit tests for all three HP reduction branches

**D2 Files changed:**
- `app/types/combat.ts` — canonical HpReductionType definition
- `app/server/services/combatant.service.ts` — re-export instead of local definition
- `app/server/api/encounters/[id]/damage.post.ts` — import from canonical location
- `app/components/encounter/CombatantGmActions.vue` — reset lossType, use imported type
- `app/components/encounter/CombatantCard.vue` — use imported HpReductionType
- `app/components/gm/CombatantSides.vue` — use imported HpReductionType
- `app/stores/encounter.ts` — use imported HpReductionType
- `app/composables/useEncounterActions.ts` — use imported HpReductionType
- `app/composables/useEncounterCombatActions.ts` — import from ~/types instead of service
- `app/tests/unit/services/combatant.service.test.ts` — 21 new lossType tests
