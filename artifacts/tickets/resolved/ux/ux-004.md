---
ticket_id: ux-004
priority: P3
severity: LOW
status: in-progress
domain: player-view
source: rules-review-149 R2
created_by: slave-collector (plan-20260225-130000)
created_at: 2026-02-25
---

# ux-004: Wire getDisplayHp into VTTToken for player mode HP masking

## Summary

In player mode, VTTToken shows the exact HP bar fill for all combatants including enemies. The `getDisplayHp` and `getInfoLevel` utilities already exist in `usePlayerGridView.ts` but are not wired into VTTToken. A motivated player could deduce exact enemy HP from pixel-level analysis of the bar width.

The design spec's information asymmetry table specifies: Own = full info, Allied = name + exact HP, Enemy = name + HP percentage only.

## Affected Files

- `app/components/vtt/VTTToken.vue` — lines 157-163 (HP bar calculation)
- `app/composables/usePlayerGridView.ts` — `getDisplayHp` and `getInfoLevel` utilities (built but unused)

## Current Behavior

HP bar fill width is `entity.currentHp / entity.maxHp` for all combatants, showing exact proportional HP for enemies.

## Expected Behavior

In player mode, enemy token HP bars should use the percentage-based display from `getDisplayHp()` (which rounds to 25% increments: full, 75%, 50%, 25%, critical, fainted).

## Impact

Low visual impact since the HP bar is proportional (not numeric), and the same behavior exists on the Group View. PTU does not mandate HP hiding. However, the utility function exists and should be connected for design spec compliance.

## Suggested Fix

Pass `playerMode` and `getDisplayHp` to VTTToken. When in player mode and the combatant is an enemy, use the rounded percentage value instead of exact HP for the bar fill calculation. Defer to P2 implementation.

## Resolution Log

| Date | Commit | Description |
|------|--------|-------------|
| 2026-02-25 | e3facc8 | `usePlayerGridView.ts`: Added `roundToDisplayTier()` that snaps enemy HP to 25% increments (100, 75, 50, 25, 10, 0) |
| 2026-02-25 | dabee52 | `VTTToken.vue`: Added `displayHpOverride` prop — when provided, overrides exact HP for bar fill |
| 2026-02-25 | a9fb82b | `GridCanvas.vue`: Added `getDisplayHpOverride()` that computes rounded HP for enemies in playerMode, wired to VTTToken via `:display-hp-override` |
| 2026-02-26 | ddcd8d9 | Extract `roundToDisplayTier` to `app/utils/displayHp.ts`, import in `GridCanvas.vue` (code-review-167 H1) |
| 2026-02-26 | ad2734e | Remove dead `getDisplayHp` from `usePlayerGridView.ts` — GridCanvas owns HP masking (code-review-167 M1) |

### Files Changed
- `app/utils/displayHp.ts` — New shared utility with `roundToDisplayTier()` (single source of truth)
- `app/composables/usePlayerGridView.ts` — Removed local `roundToDisplayTier()` and dead `getDisplayHp()`
- `app/components/vtt/VTTToken.vue` — Added `displayHpOverride` prop, `hpPercent` computed uses it when provided
- `app/components/vtt/GridCanvas.vue` — Imports `roundToDisplayTier` from shared utility, removed local duplicate

### Verification
- `GroupGridCanvas.vue` does NOT pass `playerMode` to GridCanvas, so `getDisplayHpOverride` returns `undefined` for all tokens. Group View continues showing exact HP bars. NOT affected.
- `PlayerGridView.vue` passes `:player-mode="true"` and player ownership IDs. Enemy tokens get rounded HP. Own/allied tokens get exact HP.
- IsometricCanvas does not use VTTToken, so isometric mode is unaffected.
