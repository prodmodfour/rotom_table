---
id: bug-032
title: No levelMin <= levelMax validation in encounter tables
priority: P4
severity: LOW
status: in-progress
domain: encounter-tables
source: encounter-tables-audit.md (OBS-001)
created_by: slave-collector (plan-20260226-175938)
created_at: 2026-02-26
---

# bug-032: No levelMin <= levelMax validation in encounter tables

## Summary

No cross-validation exists in table or entry creation APIs for level range fields. If `levelMin > levelMax`, the level generation formula produces a negative range, resulting in unpredictable levels.

## Affected Files

- `app/server/api/encounter-tables/` (creation and update endpoints)

## Suggested Fix

Add Zod validation: `levelMin` must be <= `levelMax`. Return 400 error if violated.

## Impact

Edge case — only occurs if GM manually enters invalid level ranges. Results in unpredictable Pokemon levels.

## Resolution Log

- **Commit:** `0272c1d` — fix: add levelMin <= levelMax validation to encounter table APIs
- **Files changed (8):**
  - `app/server/api/encounter-tables/index.post.ts` — table creation validation
  - `app/server/api/encounter-tables/[id].put.ts` — table update validation
  - `app/server/api/encounter-tables/[id]/entries/index.post.ts` — entry creation validation
  - `app/server/api/encounter-tables/[id]/entries/[entryId].put.ts` — entry update validation (merges with existing DB values)
  - `app/server/api/encounter-tables/[id]/modifications/index.post.ts` — modification creation validation
  - `app/server/api/encounter-tables/[id]/modifications/[modId].put.ts` — modification update validation
  - `app/server/api/encounter-tables/[id]/modifications/[modId]/entries/index.post.ts` — modification entry creation validation
  - `app/server/api/encounter-tables/import.post.ts` — import validation (table, entries, modifications, modification entries)
- **Approach:** Inline cross-field validation before DB writes. Returns HTTP 400 with message `'levelMin must be less than or equal to levelMax'`. Entry update merges provided values with existing DB values to catch partial updates.
