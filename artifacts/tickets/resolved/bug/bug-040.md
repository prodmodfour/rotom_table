---
ticket_id: bug-040
title: "Extended-rest endpoint lacks Math.max(0, ...) safety clamp on currentAp calculation"
priority: P4
severity: LOW
domain: healing
source: code-review-216 MEDIUM-1 (follow-up)
created_by: slave-collector (plan-20260228-101738)
created_at: 2026-02-28
---

## Summary

The extended-rest endpoint (`app/server/api/characters/[id]/extended-rest.post.ts:97`) computes `currentAp: maxAp - character.boundAp` without clamping to zero. While the character PUT endpoint clamps `boundAp` to `[0, maxAp]`, there is no guarantee this invariant holds if data is manually edited in the database or if a future code path writes `boundAp` without clamping.

A negative `currentAp` written to the database could cause UI rendering bugs or downstream arithmetic errors.

The `calculateAvailableAp()` utility in `restHealing.ts:232-234` already applies `Math.max(0, ...)`. The extended-rest endpoint should use the same defensive pattern.

## Affected Files

- `app/server/api/characters/[id]/extended-rest.post.ts` (line ~97)

## Suggested Fix

Change:
```ts
currentAp: maxAp - character.boundAp
```
To:
```ts
currentAp: Math.max(0, maxAp - character.boundAp)
```

## Impact

LOW — requires manually corrupted DB data to trigger. Defensive fix for data integrity.

## Related

- code-review-216 MEDIUM-1: Same gap was identified and required in the new-day endpoints (fix cycle for bug-038). The extended-rest endpoint was noted as out-of-scope for that fix.

## Resolution Log

**Status:** ALREADY_FIXED
**Resolved by:** commit 3d6a238 ("fix: add Math.max(0,...) safety guard to currentAp calculation")
**Date:** Pre-existing fix on master (applied during bug-038 fix cycle)

The fix described in this ticket was already applied before the ticket was created. Line 97 of `app/server/api/characters/[id]/extended-rest.post.ts` already reads:
```ts
currentAp: Math.max(0, maxAp - character.boundAp)
```

**Duplicate code path check (all clamped):**
- `app/utils/restHealing.ts:233` -- `Math.max(0, maxAp - boundAp - drainedAp)`
- `app/server/api/characters/[id]/extended-rest.post.ts:97` -- `Math.max(0, maxAp - character.boundAp)`
- `app/server/api/characters/[id]/new-day.post.ts:40` -- `Math.max(0, maxAp - character.boundAp)`

All production code paths that compute currentAp from maxAp - boundAp are properly clamped. No further action needed.
