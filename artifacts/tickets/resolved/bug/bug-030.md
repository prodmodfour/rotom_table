---
ticket_id: bug-030
priority: P2
severity: HIGH
status: in-progress
domain: player-view
source: code-review-159 M3
created_by: slave-collector (plan-20260225-130000)
created_at: 2026-02-25
---

# bug-030: Player grid has no touch event support for mobile devices

## Summary

The player VTT grid (`GridCanvas.vue` in player mode) has zero touch event handlers. Mobile devices (the primary target audience for the player view) cannot pan or zoom the grid. The design spec P1 acceptance criteria states "Pinch-to-zoom works" but this is not implemented.

Additionally, in player mode, left-click panning is blocked because every left-click is intercepted as a cell/token click. Desktop users must use middle-click to pan, which many mice don't have.

## Affected Files

- `app/components/vtt/GridCanvas.vue` — lines 257-281 (player mode left-click interception)
- `app/composables/useGridInteraction.ts` — no touch event handlers

## Current Behavior

- Left-click in player mode always emits `playerCellClick` — no click-vs-drag detection
- No `touchstart`, `touchmove`, `touchend` handlers exist
- No pinch-to-zoom gesture recognition
- Mobile players see only the initially visible portion of the grid

## Expected Behavior

- Touch pan (single finger drag) should scroll the grid
- Pinch-to-zoom should work on mobile
- Left-click on desktop should distinguish between click (cell selection) and drag (panning)
- A tap on mobile should select a cell, a drag should pan

## Impact

Mobile players (phones/tablets) cannot navigate the tactical grid at all. This is a core usability blocker for the player view's primary use case (phone-based play during tabletop sessions).

## Suggested Fix

1. Add click-vs-drag detection: track `mousedown` position, if cursor moves >5px before `mouseup`, treat as pan instead of cell click
2. Add touch event handlers for pan (single finger) and pinch-to-zoom (two fingers)
3. Consider using a gesture library or implementing basic touch math (distance between two touch points for zoom factor)

## Resolution Log

### Fix Cycle (2026-02-26)

**Branch:** `slave/4-dev-bug-030-20260225-174854`

**Commits:**
- `7a037c8` — feat: add touch event handlers to useGridInteraction composable
- `daf0c68` — feat: wire touch events in GridCanvas.vue with player mode support
- `90b616a` — fix: add touch-action: none to prevent browser default gestures on grid
- `584a55d` — fix: handle touch token taps in composable since preventDefault suppresses click
- `2e74942` — feat: add touch event support to IsometricCanvas for consistency

**Files Changed:**
- `app/composables/useGridInteraction.ts` — Added handleTouchStart, handleTouchMove, handleTouchEnd methods with touch state tracking, pinch-to-zoom math (getTouchDistance, getTouchCenter), tap-vs-drag detection (5px threshold), and onTouchTap callback for player mode override
- `app/components/vtt/GridCanvas.vue` — Wired @touchstart/@touchmove/@touchend events, added touch-action: none CSS, added onTouchTap callback for player mode tap routing, removed obsolete TODO comment
- `app/components/vtt/IsometricCanvas.vue` — Wired touch events and touch-action CSS for consistency

**What was implemented:**
1. **Single-finger pan:** touchmove delta applied to panOffset after exceeding 5px threshold
2. **Pinch-to-zoom:** Two-finger distance ratio scales zoom level, with pan offset adjusted to zoom toward pinch center
3. **Tap detection:** Touch without movement beyond threshold treated as tap (selects cell/token)
4. **Player mode:** onTouchTap callback routes taps to playerTokenSelect/playerCellClick events
5. **touch-action: none CSS:** Prevents browser default pan/zoom gestures
6. **preventDefault on all touch events:** Prevents synthesized click events (composable handles all touch taps directly)

**Note on click-vs-drag detection:** The desktop left-click-vs-drag detection (suggested fix #1) was already implemented in commit ce8e6a4 prior to this fix cycle. This cycle focused on mobile touch support (suggested fixes #2 and #3).

### Fix Cycle 2 — code-review-166 (2026-02-26)

**Branch:** `slave/1-dev-bug-030-fix-20260226`

**Review:** code-review-166 CHANGES_REQUIRED — 3 issues (C1 CRITICAL, M1 MEDIUM, M2 MEDIUM)

**Commits:**
- `6d9ae64` — fix: add touch handlers to useIsometricInteraction composable (C1)
- `fc2e626` — refactor: deduplicate tap/click threshold constant (M1)

**Files Changed:**
- `app/composables/useIsometricInteraction.ts` — Added handleTouchStart, handleTouchMove, handleTouchEnd methods with touch state tracking, pinch-to-zoom, single-finger pan, tap detection using isometric coordinate transforms (screenToGrid with inverse isometric projection, getTokenAtScreenPosition with diamond hit-testing). Added onTouchTap callback to options interface. Methods now returned from composable to match IsometricCanvas.vue template bindings.
- `app/composables/useGridInteraction.ts` — Moved TOUCH_TAP_THRESHOLD to module-level export (was function-scoped)
- `app/components/vtt/GridCanvas.vue` — Replaced local CLICK_THRESHOLD_PX with imported TOUCH_TAP_THRESHOLD from useGridInteraction

**Issues resolved:**
- **C1 CRITICAL:** IsometricCanvas.vue bound @touchstart/@touchmove/@touchend to interaction.handleTouchStart/Move/End but useIsometricInteraction.ts did not define these methods. Any touch on the isometric grid crashed with TypeError. Fixed by implementing the three touch handlers adapted for isometric transforms.
- **M1 MEDIUM:** TOUCH_TAP_THRESHOLD=5 in useGridInteraction.ts and CLICK_THRESHOLD_PX=5 in GridCanvas.vue were duplicate constants. Exported TOUCH_TAP_THRESHOLD from the composable and imported in GridCanvas.vue.
- **M2 MEDIUM:** File size concern for useGridInteraction.ts (~765 lines). Already tracked as refactoring-082. No code action needed.
