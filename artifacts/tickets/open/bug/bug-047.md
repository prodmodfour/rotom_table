---
id: bug-047
title: Friend Ball loyalty fallback default mismatch (2 vs schema default 3)
priority: P4
severity: LOW
status: in-progress
domain: capture
source: code-review-307 MED-001
created_by: slave-collector (plan-20260303-191515)
created_at: 2026-03-03
affected_files:
  - app/server/api/capture/attempt.post.ts
---

# bug-047: Friend Ball loyalty fallback default mismatch

## Summary

In `app/server/api/capture/attempt.post.ts` line 192, the nullish coalescing fallback for loyalty is `2` (Wary), but `schema.prisma` declares `loyalty Int @default(3)` (Neutral). The fallback should be `3` to match the schema default.

## Problem

```typescript
loyalty: (pokemon.loyalty ?? 2) + 1  // fallback is 2 (Wary)
```

The schema default is 3 (Neutral). If `pokemon.loyalty` is somehow null/undefined, the fallback produces a different starting value than the database would assign.

## Impact

Unlikely to trigger in practice since Pokemon records always have loyalty set by the generator. But the mismatch is confusing and could cause issues if the code path is ever exercised.

## Suggested Fix

Change fallback from `2` to `3` to match schema default.

## Resolution Log

- `e4137af1` — fix: correct Friend Ball loyalty fallback from 2 to 3 to match schema default (`app/server/api/capture/attempt.post.ts`)
- `f06f0dca` — fix: correct intercept loyalty fallback from 0 to 3 to match schema default (`app/server/services/intercept.service.ts`)

### Codebase Audit (Lesson #2 compliance)

Grepped entire codebase for `loyalty ??` patterns. Results:

| File | Fallback | Status |
|------|----------|--------|
| `app/server/api/capture/attempt.post.ts` | `?? 2` | **Fixed to `?? 3`** |
| `app/server/services/intercept.service.ts` | `?? 0` | **Fixed to `?? 3`** |
| `app/server/utils/serializers.ts` (x2) | `?? 3` | Already correct |
| `app/server/api/pokemon/index.post.ts` | `?? 3` | Already correct |
| `app/server/services/entity-builder.service.ts` | `?? 3` | Already correct |
| `app/components/pokemon/PokemonStatsTab.vue` (x2) | `?? 3` | Already correct |
| `artifacts/designs/design-priority-interrupt-001/spec-p2.md` | `?? 0` | Design spec (not runtime code, noted only) |
